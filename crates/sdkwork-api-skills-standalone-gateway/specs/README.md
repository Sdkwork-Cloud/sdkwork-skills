# SDKWork Skills Standalone Gateway Specs

`component.spec.json` defines the standalone ingress contract. The binary enables the canonical
process-shared database pool before assembly bootstrap, then serves the Skills app and backend API
surfaces with liveness, readiness, and graceful shutdown.
