version: 1
agent:
  short_description: "Legacy pointer to the top-level Node.js app email skill."
  default_prompt: "Use the repo-root sdkwork-skills-app implementation of $sdkwork-email and the shared Node.js runtime under .sdkwork-skills-app-shared."
