interface:
  display_name: "Sdkwork Auth Core"
  short_description: "Reusable app-v3 authentication skill with progressive OpenAPI endpoint loading."
  default_prompt: "Use $sdkwork-auth-core to register/login/refresh, persist ~/.sdkwork/user/auth.json, and output reusable auth headers for other skills."
