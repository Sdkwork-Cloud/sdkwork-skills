version: 1
agent:
  short_description: "Legacy pointer to the top-level Node.js app auth skill."
  default_prompt: "Use the repo-root sdkwork-skills-app implementation of $sdkwork-auth-core and the shared Node.js runtime under .sdkwork-skills-app-shared."
